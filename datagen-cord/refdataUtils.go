package main

/*

 */
func NormalizeAssetSymbols(raw []AssetSymbol) (normalized []AssetSymbol) {

	/// todo rm whitespace, casing and special chars from asset symbol raw queried data
	return normalized
}
